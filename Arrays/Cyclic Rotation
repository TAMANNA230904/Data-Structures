Given an array, rotate the array by one position in clock-wise direction.
 

void rotate(int arr[], int n)
{
    int i=n-2;
    int last=arr[n-1];
    while(i>=0){
       arr[i+1]=arr[i];
       i--;
    }
    arr[0]=last;
}
